# --- Configuração do Banco de Dados PostgreSQL ---
# Estas variáveis são usadas pelo serviço 'postgres-db' e 'sunops-api' no docker-compose.yml

# Nome de usuário para o banco de dados PostgreSQL
DATABASE_USERNAME=sunops_user

# Senha para o usuário do banco de dados (use uma senha forte!)
DATABASE_PASSWORD=SenhaMuitoForteParaOBancoDeDados123!

# Nome do banco de dados que será criado
DATABASE_NAME=sunops_db

# Host do banco de dados (usado pela API para se conectar ao container do DB)
# Geralmente é o nome do serviço definido no docker-compose.yml
DATABASE_HOST=postgres-db


# --- Configuração da API FastAPI (Backend) ---
# Estas variáveis são lidas pelo seu backend (config.py, auth/services.py)

# Chave secreta para assinatura dos tokens JWT (JSON Web Tokens)
# Use uma string longa, complexa e aleatória. NÃO COMPARTILHE!
# Exemplo gerado (substitua por um seu):
SECRET_KEY=testsecret1234567890testsecret1234567890abcdefghi

# Algoritmo usado para assinar os tokens JWT (geralmente HS256)
ALGORITHM=HS256

# Tempo de expiração do token de acesso em minutos (ex: 60 para 1 hora)
ACCESS_TOKEN_EXPIRE_MINUTES=60


# --- Outras Configurações (Opcional) ---

# Ambiente da aplicação (development, production, etc.) - Pode ser usado no seu código Python
APP_ENV=development

# Credenciais do primeiro usuário Gestor
FIRST_ADMIN_EMAIL=gestor@sunops.com
FIRST_ADMIN_PASSWORD=admin123
FIRST_ADMIN_NAME=Gestor Principal